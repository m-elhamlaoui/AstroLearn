apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - postgres.yaml
  - backend.yaml
  - frontend.yaml

# Common labels to be applied to all resources
commonLabels:
  app.kubernetes.io/name: astrolearn
  app.kubernetes.io/part-of: astrolearn-app
